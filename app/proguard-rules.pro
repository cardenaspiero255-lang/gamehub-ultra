# GameHub Ultra release uses R8 shrinking and resource shrinking.
# Keep rules should be added only when a release build demonstrates a concrete
# reflection/serialization issue; no blanket keep rules are used.
